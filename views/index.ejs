<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title> Quoting Dojo </title>
    </head>
    <body>
        <h1>
            Welcome to Quoting Dojo 
        </h1>
        <form action="/quotes" method="POST">
            <div>
                <label for="name">
                    Your Name:
                </label>
                <input type="text" name="name" id="name">
            </div>
            <div>
                <label for="qoute">
                    Your Quote:
                </label>
                <textarea name="qoute" id="qoute"></textarea>
            </div>
            <button type="submit"> 
                Add my quote!
            </button>
        </form>
        <a href="/quotes">
            <button>Skip to quotes!</button>
        </a>

        <div class="error">
            <% if( messages.start ){ %>
                <%= messages.start %>
            <% } %>
        </div>
        <div class="error">
            <% if( messages.fail ){ %>
                <%= messages.fail %>
            <% } %>
        </div>
    </body>
</html>